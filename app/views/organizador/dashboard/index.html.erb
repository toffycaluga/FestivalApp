<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Dashboard</h1>
    <%# <a href="#" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm"><i
            class="fas fa-download fa-sm text-white-50"></i> Generate Report</a> %>
</div>
                    <!-- Content Row -->
<div class="text-center">
  <h2><%= @festival.name %></h2>
  <p><%= @festival.description %></p>
</div>
    <%= render 'organizador/shared/card_metricas',text:"Total Postulaciones",count: @total_postulaciones, color:"success"%>  
<div class="row">

<h2>Total de Postulaciones por Categoría</h2>
<% @postulaciones_por_categoria.each do |categoria, total| %><%category=Category.find(categoria)%>
    <%= render 'organizador/shared/card_metricas',text:category.name,count: total, color:"primary"%>   
<% end %>
    
</div>


