<!-- En app/views/admin/festivals/personas_que_quedaron.html.erb -->
<h1>Personas que Quedaron en el Festival</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>
      <th>telefono</th>
    </tr>
  </thead>
  <tbody>
        
        <% if @personas_que_quedaron != [] %>
            
    
        
        <% @personas_que_quedaron.each do |persona| %>
        <tr>
            <td><%= persona.name %></td>
            <td><%= persona.email %></td>
            <td><%= persona.phone_number %></td>
        </tr>
        

        <% end %>
    <% else  %>
        <p>Aún no hay participantes seleccionados</p>
    <% end %>
    
  </tbody>
</table>

<% if @festival %>
  <%= link_to 'Sleccionar Participantes', editar_estado_postulacion_admin_festival_path(@festival), class: 'btn btn-primary mt-3' %>
<% end %>
<br>
<%= link_to 'Volver al Festival', admin_festival_path(@festival) %>